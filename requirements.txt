# cosmic-replay v4 - Python Dependencies

# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pyyaml>=6.0
requests>=2.31.0
urllib3>=2.0.0
pycryptodome>=3.15.0       # RSA 密码加密（登录必需）

# Observability (Prometheus 指标导出；缺失时 server.py 优雅降级，不影响核心功能)
prometheus-client>=0.19.0

# Test dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Optional browser exploration for safe Kingdee menu/form discovery
playwright>=1.44.0
