/**
 * This is a kingdee cosmic template project that is automatically generated by the Kingdee cosmic development assistant plugin. 
 * If there are any issues during the use process, you can provide feedback to the kingdee developer community website.
 * Website: https://developer.kingdee.com/developer?productLineId=29
 * Author: liebin.zheng
 * Generate Date: 2025-07-21 14:36:01
 */
rootProject.name = System.getProperty('artifactId')

include(
		':${ISV_FLAG}-cosmic-debug',
		':${ISV_FLAG}-base-common',
		':${ISV_FLAG}-base-helper',
		
		':${ISV_FLAG}-odc-homs',

)

// -------------- 引入苍穹调试工程模块 --------------
project(':${ISV_FLAG}-cosmic-debug').projectDir = new File('code/${ISV_FLAG}-cosmic-debug')

// -------------- 引入公共基础模块 --------------
project(':${ISV_FLAG}-base-common').projectDir = new File('code/base/${ISV_FLAG}-base-common')
project(':${ISV_FLAG}-base-helper').projectDir = new File('code/base/${ISV_FLAG}-base-helper')

// -------------- 引入hr云-hdm应用模块 --------------
project(':${ISV_FLAG}-odc-homs').projectDir = new File('code/odc/${ISV_FLAG}-odc-homs')

